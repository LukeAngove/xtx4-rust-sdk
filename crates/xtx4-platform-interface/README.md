# xtx4-platform-interface

Core types and the `Platform` trait. All backends implement this trait.

- `Platform` — display, buttons, time, power management
- `Framebuffer` — 480×800 1bpp buffer
- `Buttons` — bitflags for the 7 device buttons
- `Rectangle`, `DrawTransform` — geometry types
