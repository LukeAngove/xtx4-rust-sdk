# xtx4-desktop

Desktop emulator backend. Renders the 1bpp framebuffer to an 800×480 minifb
window with ghosting and flash animations. Implements `Platform` directly.

Button mapping: D/F/J/K/L/; → device buttons.
